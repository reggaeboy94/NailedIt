<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="academy.learnprogramming.nailedit">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.sensor.stepcounter" />
    <uses-feature android:name="android.hardware.sensor.stepdetector" />
    <uses-feature
        android:name="android.hardware.senson.proximity"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:screenOrientation="portrait"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StatsActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".TrainingActivity">
            android:parentActivityName=".MainActivity"
        </activity>
        <activity android:name=".StaminaActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".StrengthActivity" />
        <activity android:name=".WillPowerActivity" />
        <activity android:name=".ToDoListActivity" />
        <activity android:name=".AchievementsActivity" />
        <activity android:name=".HelpActivity"/>
    </application>

</manifest>